spring:
  application:
    name: course-management-system

  datasource:
    url: jdbc:postgresql://localhost:5432/course_management_system
    username: postgres
    password: 123

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

server:
  port: 8080