spring:
  application:
    name: course-management-system

  datasource:
    url: jdbc:postgresql://localhost:5432/course_management_system
    username: postgres
    password: 123

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

jwt:
  secret: your_super_secret_key_your_super_secret_key
  expiration: 86400000